{
  "name": "backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "lint": "eslint ."
  },
  "dependencies": {
    "@repo/apikey": "workspace:*",
    "@repo/db": "workspace:*",
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "hono": "^4.1.7",
    "reflect-metadata": "^0.2.2",
    "typedi": "^0.10.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}